Flask>=3.0
pandas>=2.0
openpyxl>=3.1
reportlab>=4.0
gunicorn>=21.2
python-dotenv>=1.0

# Gemini live mode for the Edu-AI chatbot (degrades to offline replies when
# GEMINI_API_KEY is not set).
google-generativeai>=0.8.4

# Optional - for production parity with the thesis's original design.
# Uncomment and set config.USE_MONGO = True to enable.
# pymongo>=4.6
